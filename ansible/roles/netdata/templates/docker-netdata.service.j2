[Unit]
Description=Docker Netdata
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a docker-netdata
ExecStop=/usr/bin/docker stop -t 2 docker-netdata

[Install]
WantedBy=multi-user.target